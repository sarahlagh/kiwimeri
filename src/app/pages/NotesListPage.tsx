import { IonPage } from '@ionic/react';
import NoteList from '../components/NoteList';

const NotesListPage = () => {
  return (
    <IonPage>
      <NoteList></NoteList>
    </IonPage>
  );
};
export default NotesListPage;
